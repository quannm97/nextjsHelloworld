'use client';
import React from "react";

const AddToCart = () => {
    return (
        <div>
            <button onClick={() => console.log(1)}>Click </button>
        </div>
    );
};

export default AddToCart;
